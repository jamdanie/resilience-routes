# Optional. Leave unset to show a truthful local "Live session" state without a global count.
# The endpoint must accept heartbeat POST requests and return JSON such as: {"active": 3}
VITE_PRESENCE_ENDPOINT=
